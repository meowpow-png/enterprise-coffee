<configuration>
    <property name="COFFEE_LOG_LEVEL" value="${COFFEE_LOG_LEVEL:-INFO}" />
    <property name="LOG_PATTERN" value="[%d{yyyy-MM-dd HH:mm:ss.SSS}] [%-5level] [%thread] [%logger{0}] %msg%n"/>
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>${LOG_PATTERN}</pattern>
        </encoder>
    </appender>
    <logger name="io.github.meowpowpng.enterprisecoffee" level="${COFFEE_LOG_LEVEL}" />
    <root level="INFO">
        <appender-ref ref="CONSOLE"/>
    </root>
</configuration>
